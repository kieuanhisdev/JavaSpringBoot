server:
    port: 8080
    servlet:
        context-path:

spring:
    datasource:
        url: "jdbc:mysql://localhost:5433/learn"
        username: root
        password:
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true